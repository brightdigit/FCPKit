---
name: v0.1.0-plan-and-dsl
description: Accepted v0.1.0 plan (ADR 0002) and locked FCPKitDSL surface; where to resume
metadata:
  node_type: memory
  type: project
---

# v0.1.0 plan & DSL (accepted 2026-07-28)

## Status

- **ADR 0002** accepted: create-first, ordered content models, strong types, `FCPKitDSL`, `FCPKitScripting`.
- **Implementation not started.** Planning lives on branch/worktree `swift-package-plan`.
- Code work targets **`v0.1.x`** worktree — merge/cherry-pick planning+ADR commits there before Step 0.

## Resume reading order

1. `docs/adr/0002-create-first-ordered-typed-model.md`
2. `docs/planning/v0.1.0-first-working-version.md` (§3 = locked DSL)
3. `docs/planning/v0.1.0-investigation-findings.md` (§8–10 decisions + deferred grill)
4. `docs/NEXT_STEPS.md` → “Resume later”

## Immediate next code

**Step 0:** XMLCoder ordering tests + Spine choice spike + `XCTExpectFailure` order assert on
`FeaturePairs/transitions/after.fcpxml`. Then Steps 1–5 (types → ordered Spine → story/resources),
**Step 6 = `FCPKitDSL`**.

## DSL locked (do not re-litigate without user)

Products: `FCPKit` / `FCPKitDSL` / `FCPKitScripting`.  
`Document` + `export(version:) -> FCPXML`. Soft-promote shells; hide primary `Spine`.  
`.anchor(lane:content:)`. Inline media + spec dedupe. Pack offsets + transition overlap;
modifiers override. No media stored properties on `Document`. MediaTools multicam stays Codable
until after 0.1.0.

Deferred grill: markers/retiming modifiers, time literals, RefClip/media, overlap algorithm
detail, preset catalog, URL vs path, title `start` default — see findings §10.
