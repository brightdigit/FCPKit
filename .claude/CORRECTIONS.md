# Corrections Log

Append-only. Source of truth for user corrections and explicit always/never directives.
One concise line per entry. Never rewrite, reorder, or delete prior entries.

- 2026-07-17: Always append one concise line here whenever the user corrects me or gives an explicit "always"/"never" directive; preserve all prior entries, never rewrite/reorder/delete.
- 2026-07-17: Keep this corrections log in the repo in an agent-visible folder (`.claude/`), not machine-only, so it is committed and shared.
- 2026-07-17: Save memories AND corrections in the repo (`.claude/memory/` and `.claude/CORRECTIONS.md`), never only in the local user memory directory.
