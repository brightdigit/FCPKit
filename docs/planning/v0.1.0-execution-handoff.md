# v0.1.0 execution handoff — state as of 2026-07-30

Session recovered after Antigravity API overload mid–Step 5. Live execution
state for [v0.1.0-worktree-plan.md](v0.1.0-worktree-plan.md), amended by two
user decisions: **maximize parallel worktree work**, and **one PR per step; the
user reviews and squash-merges every PR** (agents never merge into `v0.1.x`).

## Done

| Item | Result |
| --- | --- |
| #4 Scaffolding | Squash-merged as `3e59e49` (PR #15). Issue closed. `v0.1-scaffold` branch/worktree deleted. |
| #5 Step 0 guardrails | Squash-merged as `a1954ba` (PR #17). Issue closed. Post-merge gate verified green on `v0.1.x` (65 tests, completeness 0). |
| #6 Step 1 value types | Squash-merged as `dd6a3c8` (PR #18). Issue closed. Post-merge gate verified green on `v0.1.x`. |
| #7 Step 2 leaf types | Squash-merged as `0c3b23f` (PR #20). Issue closed. Post-merge gate verified green on `v0.1.x`. |
| Integration worktree | `v0.1.x/` exists for post-merge gate runs; never commit implementation there. |

### Step 0 spike verdict (binding on #8)

`items = ""` + `XMLChoiceCodingKey` + `DynamicNodeEncoding` does **not** fully
compose in XMLCoder 0.17.1: synthesized **decode** of the empty-string key
silently yields an empty array. Synthesized **encode** composes fine.
Therefore #8 keeps synthesized `encode(to:)` with `items = ""` but hand-writes
`init(from:)` (MixedEitherSide pattern: attributes via keyed container, items
via `singleValueContainer().decode([SpineItem].self)`). Pinned by passing tests
in `Tests/FCPKitTests/ChoiceEncodingSpike.swift`; also commented on #8.
#8 must also **remove the strict `XCTExpectFailure` wrapper** (and its
`#if canImport(ObjectiveC)` gate) in `Tests/FCPKitTests/SpineOrderTests.swift`.

## In flight

| Item | State |
| --- | --- |
| #12 Scripting | **PR #21 open** (base `v0.1.x`, head `v0.1-scripting`). macOS-only additive scripting. |
| #8 Step 3 ordered Spine | **PR #22 open** (base `v0.1.x`, head `v0.1-step3`). Gate green on branch. |
| #9 Step 4 story elements | **PR #23 open** (should base `v0.1-step3`, head `v0.1-step4`). AnchoredItem + DTD-order CodingKeys. |
| #10 Step 5 resources | **In progress / PR pending** (`v0.1-step5`). ResourceID/ResourceRef + typed shells; gate green locally. |

## Not started (queue, in dependency order)

Wave topology (each wave parallel; branch names double as worktree names,
create with `git trees add <branch> <base>` from repo root):

| Wave | Branch | Issue | Base | PR base |
| --- | --- | --- | --- | --- |
| 6 | `v0.1-step6` | #11 FCPKitDSL | `v0.1-step5` | `v0.1-step5` |

Then #13 docs sync and #14 human Final Cut gate (not agent work).

### Step notes (beyond the issue bodies)

- **#8**: hand-written `init(from:)` per the spike verdict above; synthesized
  `encode(to:)` with `items = ""` stays. Migrate test call sites; extend
  `testEveryModelTypeDeclaresNodeEncoding`; flip `SpineOrderTests`.
- **#9**: DTD-order `CodingKeys` on story elements; add permanent raw-tree
  order tests across all five FeaturePairs plus byte-stability tests
  (every `%time;` attribute character-identical after round-trip).
- **#10**: `Asset.audioRate` stays `String` (`"48000"`); only `Sequence`/
  `Multicam` use the `AudioRate` enum (`"48k"`). Don't mix them.
  `ExpressibleByStringLiteral` on `ResourceID`/`ResourceRef`/`XMLAttributeCase`/
  `FCPBool` was added so call sites can keep `"r1"` / `"NDF"` literals; unknown
  failable-init tests must route through a `String` variable (literal form
  precondition-fails).
- **#11**: acceptance is DSL-rebuilt `transitions` + `titles` FeaturePairs →
  encode → DTD-validate when the FCP DTD is present (else record the skip) →
  normalize → structurally equal. Resource ids `r1…rN` in first-reference order.
- **#12**: macOS-only, additive product/target, protocol-shimmed over
  `SBObject`, `XCTSkip`/skip when FCP absent, `#if os(macOS)` gated. If it
  needs model-owned files: park the lane, comment on #12, continue the chain.

### Fork behavior (unattended)

| Situation | Action |
| --- | --- |
| #8 breaks the completeness gate or a fixture contradicts the DTD | Pause chain, report on #8 |
| CI red on a stacked PR | Fix on the owning branch, propagate up via plain merges, continue |
| User merges modified or out of order | Pause the lane, comment on the PR; never auto-rebase over user edits |

### Test framework conflict (resolve in #13)

`.claude/agent-notes.md` mandates Swift Testing for new tests; the accepted
plan §8 says land v0.1.0 on XCTest. #5 followed the plan (XCTest); #6 followed
agent-notes (Swift Testing). Both gates are green; pick one and reconcile.

## Protocols (keep these)

- **Gate before every PR** (in the step worktree):
  `LINT_MODE=STRICT ./Scripts/lint.sh` (auto-formats — run before the final
  test pass), `swift build && swift test`, then
  `swift run fcpxml-diff schema-completeness Tests/FCPKitTests/TestData --fail-if-total-exceeds 0`.
- **Stacked PRs:** base = predecessor branch; merge bottom-up only;
  squash-merge unmodified and delete the head branch so GitHub retargets the
  child PR to `v0.1.x` (if not deleted, retarget manually with
  `gh pr edit <n> --base v0.1.x`).
- **Merges into `v0.1.x` don't auto-close issues** (non-default branch): after
  each merge, ff `v0.1.x`, re-run the gate there, close the issue with the
  landing SHA.
- **Fix propagation:** a post-branch fix on step *k* is merged (plain `git
  merge`, never rebase/force-push) into every descendant branch immediately.
- PR bodies: `Part of #N`, scope, pasted gate results, merge-train note.
